// const prompt = require("prompt-sync")()

var helloWorld = "hello"
helloWorld = "world"
console.log(helloWorld)
// This type of variables can be changerd over the time

let x = 5
x += 4
console.log(x)
// This type of the variable is similar to the var, this one can also be changed later on

const VELOCITY = 34
console.log(VELOCITY)
// This type of the variable is constant and can be changed over the time, even if we want to